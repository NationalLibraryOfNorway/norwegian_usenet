repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.14.11
  hooks:
    # Run the linter with isort on top of ruff's defaults, fixing what it can.
    - id: ruff-check
      args: [--extend-select, I, --fix]
    # Run the formatter.
    - id: ruff-format